# План запуска бота CS2ResultsBot на Yandex Cloud Functions

## TL;DR
Ниже представлен доработанный, «железный» план, позволяющий менеджеру без сильных навыков программирования и DevOps довести проект до продакшена с помощью Yandex Cloud Functions. План охватывает все этапы: от получения токена и прав бота в канале, настройки `chat_id`, до расписания (Trigger Timer), логирования, формата сообщений, обработки ошибок и проверки, что посты действительно попадают в канал.

## Структура репозитория
Исполняемый код функции лежит в каталоге `src/`: файл `main.py` содержит обработчик для Yandex Cloud Functions и сборку сообщений, а `hltv_client.py` инкапсулирует обращение к неофициальному HLTV API. Рядом в корне лежит `requirements.txt` с зависимостями функции и README с подробным планом развёртывания.

Инфраструктурные артефакты собраны в `.github/workflows/deploy.yml`, который автоматически пакует и заливает функцию при пуше в `main`. Для локальной разработки и деплоя используем рабочие ветки формата `dev/*`, а продакшн код живёт в `main` и пополняется только через Pull Request.

---

## 1. Базовая подготовка
**User Story:** как владелец проекта, я хочу иметь оформленного бота и права в канале, чтобы бот мог публиковать сообщения.

**Шаги:**
1. Создай бота через [@BotFather](https://t.me/BotFather) (команда `/newbot`) и сохрани полученный Bot Token.
2. Добавь бота в нужный Telegram-канал и выдай ему права администратора с правом «Публиковать сообщения».
3. Зафиксируй username канала (например, `@my_channel`). Это упростит отправку сообщений (не нужно узнавать `chat_id`).
4. Если у канала нет username, создай публичный `@username` в настройках канала. Альтернатива — получить числовой `chat_id` канала вида `-100...` через метод `getUpdates` после добавления бота, но использование `@username` проще.

**Критерий приёмки:** бот отображается в списке админов канала; у канала задан `@username` (или определён `chat_id`), позволяющий адресовать сообщения.

---

## 2. Архитектура и способ запуска
**User Story:** как менеджер, я хочу простой и недорогой способ автоматических постов, чтобы не держать свой сервер.

**Шаги:**
1. Используем Yandex Cloud Functions (serverless) – это позволит запускать код бота без собственного сервера.
2. Никаких webhooks и polling не нужно: функцию будет запускать по расписанию Timer Trigger (крон-расписание).
3. При каждом срабатывании функция:
   - Делает запрос к неофициальному API результатов: `https://hltv-api.vercel.app`.
   - Формирует текстовые сообщения с результатами матчей.
   - Отправляет их в канал через Telegram Bot API методом `sendMessage`, используя `chat_id` канала (наш `@username`).

**Критерий приёмки:** в Yandex Cloud создана Cloud Function и привязан триггер по расписанию (например, срабатывание каждый час).

---

## 3. Формат сообщений и минимальная логика
**User Story:** как админ канала, я хочу, чтобы посты были читабельны, информативны и единообразны.

**Шаги:**
1. Согласовать шаблон сообщения. Например, минимально достаточный формат:
   ```
   🆚 {team1} vs {team2}
   📅 {date_local}
   🗺 {map} — {score}
   Источник: HLTV
   ```
2. На первом этапе не реализуем устранение дубликатов матчей (повторная отправка одинаковых результатов допустима на данном этапе).
3. Если API не вернул результатов или вернул пустой/невалидный JSON – бот ничего не публикует, а пишет информацию об этом в лог.

**Критерий приёмки:** сообщения в канале соответствуют утверждённому шаблону и содержат актуальные данные о матчах.

---

## 4. Подготовка окружения и секретов
**User Story:** как менеджер, я хочу хранить токены безопасно, чтобы не хардкодить секретные данные в коде.

**Шаги:**
1. В настройках созданной функции Yandex Cloud добавь переменные окружения:
   - `TELEGRAM_TOKEN` — токен бота (полученный от BotFather).
   - `TELEGRAM_CHAT_ID` — идентификатор чата (например, `@my_channel` или числовой ID).
2. В коде функции считывай эти значения через `os.environ.get(...)` и не добавляй токен в код напрямую.
3. Пример: `TOKEN = os.environ.get("TELEGRAM_TOKEN")`.

**Критерий приёмки:** токен бота и идентификатор канала не фигурируют в тексте функции; при необходимости они меняются через настройки, а не через правку кода.

---

## 5. Логика Cloud Function (что она делает)
**User Story:** как владелец продукта, я хочу, чтобы один запуск функции выполнял полный цикл: «получить данные → сформировать сообщение → отправить в канал».

**Шаги (внутри handler):**
1. Считать из переменных окружения `TELEGRAM_TOKEN` и `TELEGRAM_CHAT_ID`.
2. Сделать HTTP GET запрос к `https://hltv-api.vercel.app/api/results` (эндпоинт последних результатов матчей).
3. Распарсить полученный JSON. Пройтись по первым `N` записям (для начала взять `N = 3`, чтобы не перегружать канал).
4. Из каждой записи извлечь интересующую информацию: команды, счёт, карту, время матча и пр.
5. Сформировать строку сообщения по шаблону (см. шаг 3).
6. Для каждого сформированного `text` вызвать Telegram API:
   - `POST https://api.telegram.org/bot<TELEGRAM_TOKEN>/sendMessage`
   - Параметры: `chat_id=TELEGRAM_CHAT_ID`, `text=text`, `parse_mode=HTML`, `disable_web_page_preview=true`.
7. Обернуть вызовы API в конструкцию `try/except`. В случае ошибки вызвать `logging.error` или аналог для сохранения информации об ошибке.
8. По окончании работы вернуть `{"statusCode": 200}` либо `{"statusCode": 500, "body": <описание ошибки>}` — чтобы Cloud Function корректно отражала статус.

**Критерий приёмки:** при тестовом запуске функции (вручную) она получает данные от API, формирует 1–3 сообщения и успешно отправляет их в канал (видно новые посты).

> Примечание: Мы сознательно не используем базу данных для хранения отправленных матчей и не делаем умную проверку на дубликаты на этом этапе — это упрощает старт.

---

## 6. Развёртывание функции в Yandex Cloud
**User Story:** как менеджер, я хочу развернуть код без поднятия собственного сервера и обеспечить его регулярный запуск.

**Шаги:**
1. В консоли Yandex Cloud открой раздел **Cloud Functions** и создай новую функцию:
   - Рантайм: Python 3.11 (подходит для использования библиотеки `requests`).
   - Точка входа: `main.handler` (если файл назван `main.py`, а в нём функция `handler`).
2. Загрузить код функции:
   - Скопируй код из локального файла (с логикой из шага 5) в редактор веб-консоли Yandex Cloud Functions.
   - Добавь файл `requirements.txt` (с указанием `requests`, если используется эта библиотека).
3. Укажи переменные окружения `TELEGRAM_TOKEN` и `TELEGRAM_CHAT_ID` (см. шаг 4) в настройках функции.
4. Нажми **Deploy** (Развернуть) для сохранения и активации новой версии функции.

**Критерий приёмки:** функция успешно развернута (статус «Активна»/`Ready`), ошибок при деплое нет.

---

## 7. Настройка расписания (Timer Trigger)
**User Story:** как менеджер, я хочу, чтобы бот публиковал результаты автоматически по расписанию, без ручного запуска.

**Шаги:**
1. В Yandex Cloud открой раздел **Triggers** и создай новый триггер типа Timer.
2. Задай расписание в формате cron. Например, чтобы запускать функцию каждый час: `0 * * * *` (каждый час в ноль минут).
3. Привяжи триггер к созданной Cloud Function.
4. Сохрани настройки триггера.

**Критерий приёмки:** триггер активно отображается в списке и указано время следующего срабатывания; функция запускается автоматически по заданному расписанию (видно по появлению новых сообщений в канале или по записям в логах).

---

## 8. Проверка и приёмка результата
**User Story:** как владелец, я хочу убедиться, что всё работает корректно, прежде чем оставить бота без присмотра.

**Шаги:**
1. Инициируй тестовый запуск функции вручную через интерфейс Yandex Cloud (кнопка «Выполнить»).
2. Убедись, что в Telegram-канале появились ожидаемые сообщения с результатами матчей.
3. Проверь логи Cloud Function: там должны быть записи об успешном выполнении (статус 200) и, при необходимости, детали отправленных запросов.
4. Если требуется, скорректируй шаблон сообщений или логику, задеплой функцию заново и протестируй повторно.

**Критерий приёмки:** после ручного запуска все шаги проходят успешно: сообщения отправлены в канал, логи не содержат ошибок.

---

## 9. Логирование и наблюдаемость
**User Story:** как менеджер, я хочу быстро понимать состояние бота и видеть возможные сбои, чтобы оперативно реагировать.

**Шаги:**
1. В коде используй логирование (`print` или модуль `logging`) на ключевых этапах:
   - Старт функции.
   - Запрос к HLTV API.
   - Количество полученных матчей.
   - Отправка сообщения в Telegram.
   - Ошибки (в блоке `except`).
2. В случае HTTP-ошибки (код ответа 4xx/5xx) при запросе к HLTV или Telegram API – логируй код и текст ошибки.
3. Регулярно просматривай логи Cloud Function через веб-консоль или CLI, особенно в первые дни после запуска.
4. *(Опционально)* Настрой алертинг: например, с помощью Yandex Monitoring — уведомление, если функция завершилась с ошибкой.

**Критерий приёмки:** по логам Cloud Function прослеживается последовательность действий функции; при возникновении ошибок видно, где и почему они произошли.

---

## 10. Защита от типовых проблем
**User Story:** как владелец, я хочу, чтобы бот работал стабильно и не прекращал работу из-за предвидимых ошибок.

**Шаги (минимальные улучшения надёжности):**
1. Добавь ограничение по времени ожидания (таймаут) на внешние запросы:
   - `requests.get(..., timeout=5)` для HLTV API.
   - `requests.post(..., timeout=5)` для Telegram API.
2. Реализуй повтор запроса (retry) один раз при неудаче:
   - Оберни вызовы в цикл из 2 попыток с паузой `time.sleep(1)` между ними.
3. Установи лимит на количество сообщений за один запуск: например, отправлять максимум 3 результата, даже если API вернул больше.
4. Обрабатывай неожиданные ситуации без краша:
   - Если структура JSON изменилась и нужные поля не найдены, логируй «Unexpected response format» и заверши работу без исключения.
   - Если Telegram API вернул ошибку (например, неправильный `chat_id` или превышен лимит сообщений), также отлови исключение и залогируй его.

**Критерий приёмки:** даже при проблемах с внешними сервисами (таймауты, временная недоступность) функция завершается корректно, не зависает; в логе есть информация о сбоях, но последующие запуски триггера продолжают работу.

---

## 11. Документация для поддержки
**User Story:** как менеджер, я хочу иметь описание проекта, чтобы через месяц понимать, как всё устроено и что менять при необходимости.

**Шаги:**
1. Подготовь файл README (или документ в корпоративной Wiki) с описанием:
   - Цель бота: что он делает, какую задачу решает.
   - Используемые сервисы: Yandex Cloud Functions, Telegram Bot API, HLTV API (со ссылкой на источник).
   - Где лежит код (например, репозиторий) и как его обновить.
   - Как изменить переменные окружения (токен, канал) и расписание триггера.
   - Как просмотреть логи функции и запустить её вручную.
2. Приведи примеры: шаблон сообщения из шага 3, ссылку на исходник или документацию HLTV API, примеры команд (если бот реагирует на команды, хотя в нашем случае он только постит).
3. Опиши планы развития бота (см. шаг 14), чтобы было понимание, какой функционал можно добавить в будущем.

**Критерий приёмки:** документация создана и доступна команде; по ней другой разработчик или админ сможет воспроизвести настройку и запуск бота с нуля.

---

## 12. Пуск в эксплуатацию и мониторинг
**User Story:** как владелец, я хочу убедиться, что бот можно оставить работать на длительный период без вмешательства, и при этом получать уведомления о неполадках.

**Шаги:**
1. Убедись, что триггер (расписание) включён и функция уже выполнялась автоматически по расписанию хотя бы пару раз.
2. Проверь, что сообщения регулярно появляются в канале согласно расписанию и соответствуют новым результатам матчей.
3. В первые 1–2 дня работы регулярно мониторь логи и сам канал:
   - нет ли дублирующих постов подряд;
   - нет ли ошибок в логах Cloud Function.
4. Настрой уведомления на случай сбоев (например, подключи отправку сообщений об ошибках себе в личку через другого бота или используй Yandex Monitoring для алертов).
5. Если через несколько дней работы всё стабильно, можно снизить частоту проверок логов (например, до одного раза в день или только при получении алертов).

**Критерий приёмки:** бот проработал первые 48 часов без критических сбоев; посты публикуются по расписанию, информация актуальна, менеджер уведомлён о состоянии бота.

---

## 13. Внесённые улучшения по сравнению с исходным планом
1. **Упрощена адресация канала:** вместо получения числового `chat_id` используется прямое указание `@username` канала – это сократило время настройки.
2. **Отказ от webhook/polling:** за счёт использования триггера по времени убраны лишние сложности с входящими обновлениями; бот не нуждается во внешнем сервере для приёма событий.
3. **Ограничение количества постов за запуск:** предотвращает случайный флуд в канал, если API вернёт много результатов разом.
4. **Явные таймауты и повтор запросов:** повысили надёжность – функция не зависнет на неответившем сервисе, а единичные сетевые сбои не приведут к пропуску поста.
5. **Единообразный формат сообщений и логирование шагов:** канал выглядит аккуратно, а по логам легко отследить работу и найти проблемы.
6. **Чёткие критерии приёмки на каждом этапе:** план построен так, что каждый шаг можно проверить и подтвердить, прежде чем двигаться дальше.

---

## 14. Идеи для развития
- Добавить кэш отправленных матчей и фильтр дубликатов (например, хранить ID матчей в Object Storage или в небольшой БД).
- Сделать локализацию сообщений (русский/английский) и вынести шаблон в конфигурацию.
- Поддержать отправку дополнительных данных: лучшие игроки, статистика, ссылки на VOD.
- Реализовать ручную команду `/status`, возвращающую текущие настройки и время последнего успешного запуска.
- Добавить уведомления об ошибках напрямую в Telegram через отдельный чат или бота-«сторожа».

---

## Приложение A. Минимальный пример кода функции
```python
import os
import time
import logging
from typing import Any, Dict, List

import requests

logging.basicConfig(level=logging.INFO)

API_URL = "https://hltv-api.vercel.app/api/results"
MAX_POSTS = 3
RETRY_ATTEMPTS = 2
TIMEOUT = 5


def fetch_results() -> List[Dict[str, Any]]:
    for attempt in range(RETRY_ATTEMPTS):
        try:
            response = requests.get(API_URL, timeout=TIMEOUT)
            response.raise_for_status()
            data = response.json()
            if isinstance(data, list):
                return data
            logging.error("Unexpected response format: %s", data)
            return []
        except Exception as exc:
            logging.exception("Attempt %s: failed to fetch results", attempt + 1)
            time.sleep(1)
    return []


def send_message(token: str, chat_id: str, text: str) -> None:
    url = f"https://api.telegram.org/bot{token}/sendMessage"
    payload = {
        "chat_id": chat_id,
        "text": text,
        "parse_mode": "HTML",
        "disable_web_page_preview": True,
    }
    for attempt in range(RETRY_ATTEMPTS):
        try:
            response = requests.post(url, data=payload, timeout=TIMEOUT)
            response.raise_for_status()
            logging.info("Message sent successfully")
            return
        except Exception:
            logging.exception("Attempt %s: failed to send message", attempt + 1)
            time.sleep(1)


def build_message(entry: Dict[str, Any]) -> str:
    team1 = entry.get("team1", {}).get("name", "Unknown")
    team2 = entry.get("team2", {}).get("name", "Unknown")
    score = entry.get("result", "?")
    map_played = entry.get("maps", "?")
    date = entry.get("date", "?")

    return (
        f"🆚 <b>{team1}</b> vs <b>{team2}</b>\n"
        f"📅 {date}\n"
        f"🗺 {map_played} — {score}\n"
        "Источник: HLTV"
    )


def handler(event, context):
    logging.info("Function started")

    token = os.environ.get("TELEGRAM_TOKEN")
    chat_id = os.environ.get("TELEGRAM_CHAT_ID")

    if not token or not chat_id:
        logging.error("Missing TELEGRAM_TOKEN or TELEGRAM_CHAT_ID")
        return {"statusCode": 500, "body": "Configuration error"}

    results = fetch_results()
    logging.info("Fetched %s results", len(results))

    if not results:
        logging.info("No results to send")
        return {"statusCode": 200, "body": "No results"}

    for entry in results[:MAX_POSTS]:
        message = build_message(entry)
        send_message(token, chat_id, message)

    logging.info("Function finished")
    return {"statusCode": 200, "body": "OK"}
```

---

## Приложение B. Полезные ссылки
- [Yandex Cloud Functions — документация](https://cloud.yandex.ru/docs/functions/)
- [Yandex Cloud Triggers](https://cloud.yandex.ru/docs/functions/operations/trigger/timer-create)
- [Telegram Bot API — sendMessage](https://core.telegram.org/bots/api#sendmessage)
- [Неофициальный HLTV API](https://hltv-api.vercel.app)

---

## История изменений
- 2024-XX-XX: Добавлен подробный план запуска бота в продакшен на базе Yandex Cloud Functions.
